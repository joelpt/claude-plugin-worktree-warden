{
  "name": "worktree-warden",
  "version": "2026.06.15.1",
  "description": "Enforces worktree-first editing via a PreToolUse gate that blocks main-checkout edits (with a deliberate, time-boxed, per-repo/global opt-out exception), blocks destruction of any dirty or un-landed worktree/branch (raw git worktree remove, rm -rf, git branch -D, ExitWorktree remove), captures uncommitted WIP to recoverable bundles and detects externally-removed worktrees across sessions, audits every land/teardown/undo, surfaces a repo's git worktrees at session start (configurable), and lands chosen ones into the default branch by deterministic rebase + fast-forward with confidence-gated conflict handling and exact-state rollback. Repo-scoped \u2014 never looks cross-repo.",
  "author": {
    "name": "Joel Thornton"
  }
}
