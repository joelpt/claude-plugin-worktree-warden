# Order

Read only if land order is unclear.

- Default: older worktrees first.
- Bias toward chronology, branch content, recent `$TARGET` changes, obvious dependencies.
- If still unclear, ask the user. Keep the summary plain and short.
